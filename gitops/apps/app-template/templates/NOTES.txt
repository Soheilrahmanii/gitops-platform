Application {{ .Release.Name }} deployed to namespace {{ .Release.Namespace }}.

Access the service:
  kubectl port-forward svc/{{ include "app-template.fullname" . }} 8080:{{ .Values.service.port }} \
    -n {{ .Release.Namespace }}

Check rollout status:
  kubectl rollout status deploy/{{ include "app-template.fullname" . }} \
    -n {{ .Release.Namespace }}

View logs:
  kubectl logs -l app.kubernetes.io/name={{ include "app-template.fullname" . }} \
    -n {{ .Release.Namespace }} --tail=50 -f
